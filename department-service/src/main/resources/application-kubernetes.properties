eureka.client.enabled=true
# Define how department-service finds config-server (using K8s DNS)
# This is typically managed by spring.config.import in bootstrap.properties or application.properties
# For now, ensure no Eureka specific config for finding config-server.
# The existing spring.config.import=optional:configserver:http://config-server:8888 should work if config-server K8s service is up.

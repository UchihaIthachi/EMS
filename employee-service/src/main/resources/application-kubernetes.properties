# This profile is active when running in Kubernetes.
eureka.client.enabled=false
# Ensure Feign client for department-service uses K8s DNS.
# This should already be configured in the main application.properties or via config-server.
# feign.client.config.department-service.url=http://department-service:8081/ # This is expected to be managed by config-server or main props.
# Ensure config-server is found via K8s DNS (handled by main application.properties spring.config.import).
